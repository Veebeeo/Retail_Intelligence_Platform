# Runtime dependencies for the API container.
#
# Notably absent: statsmodels, xgboost, scikit-learn, mlflow, mlxtend, pandera
# and prophet. The API serves precomputed forecast paths rather than fitted
# model objects (see retail_intel.forecasting.serving), so there is nothing to
# unpickle but numbers and no modelling library is needed to answer a request.
#
# Pinned, because "pandas" with no version is not a reproducible environment.

pandas==2.2.3
numpy==2.1.3
scipy==1.14.1
SQLAlchemy==2.0.36
psycopg2-binary==2.9.10
pydantic==2.10.4
pydantic-settings==2.7.0
python-dotenv==1.0.1
fastapi==0.115.6
uvicorn[standard]==0.34.0
