# Training and analysis dependencies. Not needed to serve.
#
# xgboost and statsmodels are absent here on purpose -- they are in
# requirements.txt, because a champion bundle containing one of their models
# cannot be unpickled without them.
-r requirements.txt

mlflow==2.19.0
mlxtend==0.23.3
pandera==0.22.1
openpyxl==3.1.5

# Prophet ships a compiled Stan backend that fails to build on some platforms.
# It is a candidate model, not a required one -- the registry probes it at
# start-up and skips it with a warning if unavailable. Note that selecting a
# Prophet champion makes it a serving dependency too.
prophet==1.1.6
