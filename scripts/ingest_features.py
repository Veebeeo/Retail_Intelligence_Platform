"""Deprecated entry point. See ``retail_intel.data.features``.

The original README documented this file, but it never existed in the
repository — the feature engineering lived only in notebook cells, so a clone
could not reproduce the modelling table. It exists now.
"""

from retail_intel.data.features import main

if __name__ == "__main__":
    main()
